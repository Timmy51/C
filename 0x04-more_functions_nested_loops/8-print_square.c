#include "main.h"
/**
 * print_square - Afunction that draws boxes
 * @size: the length of line
 * Return: void
 */
void print_square(int size)
{
size = 0;
while (size > 0)
{
_putchar(35);
size++;
}
_putchar('\n');
}
