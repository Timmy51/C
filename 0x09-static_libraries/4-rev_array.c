#include "main.h"
/**
 * revese_array - a function that reverses arrays
 * @a: a pointer variable a
 * @n: an integer variable n
 */

void reverse_array(int *a, int n)
{
a[n] = 0;
while (n > 0)
return (a[n]);
i--;
}
